# UI Assets

This folder is reserved for lightweight UI asset references and helpers when they become reusable.

It is not a storage engine and not a public media system.

Use only for frontend presentation assets that have a clear reuse purpose.
