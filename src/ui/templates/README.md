# UI Templates

This folder is reserved for larger reusable page or workflow templates.

Templates should emerge from real product usage, not abstract design-system planning.

Possible future templates:

- capture workflow page shell
- detail page with tabs and right rail
- onboarding capture page
- workspace dashboard section group

Do not introduce a template until it reduces real duplication.

